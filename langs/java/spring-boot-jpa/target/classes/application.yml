spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jpa
    username: root
    password: goodluck2012
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      # gin或者创建数据表
      ddl-auto: update
    #控制台显示SQL
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect